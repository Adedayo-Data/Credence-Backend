{
  "name": "credence-backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "test": "npm run test:integration",
    "test:integration": "node --test --test-concurrency=1 --import tsx tests/integration/repositories.test.ts",
    "test:local": "docker-compose -f docker-compose.test.yml up -d && TEST_DATABASE_URL=postgresql://credence:credence@localhost:5433/credence_test npm run test:integration; docker-compose -f docker-compose.test.yml down",
    "coverage": "c8 --check-coverage --lines 95 --functions 95 --branches 95 --statements 95 --reporter=text --reporter=lcov --include='src/db/**/*.ts' node --test --test-concurrency=1 --import tsx tests/integration/repositories.test.ts",
    "coverage:local": "docker-compose -f docker-compose.test.yml up -d && TEST_DATABASE_URL=postgresql://credence:credence@localhost:5433/credence_test npm run coverage; docker-compose -f docker-compose.test.yml down"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.15.6",
    "c8": "^10.1.3",
    "testcontainers": "^11.8.1",
    "tsx": "^4.7.0",
    "typescript": "~5.2.2"
  }
}
